<?php
/**
 * Arquivo contendo os caminhos para as pastas da aplicação.
 */

// Caminho para o diretório raíz.
define('ROOT_PATH', __DIR__ . DS);

// Caminho para o diretório classes
define('CLASSES_PATH', ROOT_PATH. 'classes' . DS);

// Caminho para o diretório models
define('MODELS_PATH', ROOT_PATH . 'models' . DS);

// Caminho para o diretório views
define('VIEWS_PATH', ROOT_PATH . 'views' . DS);

// Caminho para o diretório controllers
define('CONTROLLERS_PATH', ROOT_PATH . 'controllers' . DS);

define('UTILITARIOS_PATH', ROOT_PATH . 'Utilitarios' . DS);

// Caminha para o diretório assets
define('ASSETS_PATH', ROOT_PATH . 'assets' . DS);

// Caminho para o diretório views
define('UPLOADS_PATH', ROOT_PATH . 'uploads' . DS);